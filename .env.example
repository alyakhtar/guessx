# MongoDB connection string
# Since MongoDB runs on the Pi host (not inside Docker), use host.docker.internal
# which maps to the host machine's IP from within the container.
MONGODB_URI=mongodb://user:password@host.docker.internal:27017/gamex?authSource=admin

# Server port (default: 8082)
PORT=8082

# Node environment
NODE_ENV=production
