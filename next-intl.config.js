/** @type {import('next-intl').Config} */
module.exports = {
    defaultLocale: 'en',
    locales: ['en', 'fr'],

    // Configure pathnames for localized routes
    pathnames: {
        '/': '/',
    },
};
